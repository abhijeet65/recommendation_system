
const neighboring_users = 3
const number_of_movies = 1000
const default_movies_length = 20 // the default number of movies required recommended by the user.

module.exports = {
    'neighboring_users' : neighboring_users,
    'number_of_movies' : number_of_movies,
    'default_movies_length' : default_movies_length
}